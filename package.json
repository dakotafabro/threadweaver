{
  "name": "threadweaver",
  "private": true,
  "version": "0.1.2",
  "workspaces": [
    "cli",
    "dashboard",
    "mcp",
    "sdk"
  ],
  "scripts": {
    "build": "npm run -w ./sdk build && npm run -w ./dashboard build && npm run -w ./mcp build && npm run -w ./cli build",
    "typecheck": "npm run -w ./sdk typecheck && npm run -w ./dashboard typecheck && npm run -w ./mcp typecheck && npm run -w ./cli typecheck",
    "dev": "npm run -w ./cli dev",
    "test": "npm run -w ./sdk test"
  },
  "engines": {
    "node": ">=20"
  }
}
